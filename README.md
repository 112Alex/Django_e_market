# Тестовое задание для стажёра WB tech (Backend) - Интернет-магазин на Django

## 🛍️ Описание проекта

Этот проект представляет собой **REST API интернет-магазина**, разработанный в качестве тестового задания для стажёра Backend-разработки в WB tech. Проект реализован с использованием Python, Django и Django REST Framework, с развертыванием через Docker Compose.

## 🛠️ Технический стек

*   **Язык**: Python 3.12
*   **Фреймворк**: Django 4+
*   **ORM**: Django ORM (PostgreSQL)
*   **API**: Django REST Framework (DRF)
*   **Авторизация**: JWT (djangorestframework-simplejwt)
*   **Документация**: drf-spectacular (Swagger/OpenAPI), README.md
*   **Email/log**: Логирование реализовано через стандартный модуль `logging`.
*   **Контейнеризация**: Docker + Docker Compose
*   **База данных**: PostgreSQL

## ⚙️ Реализованный функционал

### 🔐 Пользователи
*   Регистрация
*   Авторизация (JWT)
*   Профиль пользователя
*   Личный баланс (с возможностью пополнения)

### 🎁 Товары
*   Поля: Название, Описание, Цена, Количество на складе.
*   CRUD операции доступны только администратору.
*   Просмотр товаров доступен всем пользователям.

### 🧺 Корзина
*   Добавление, удаление, изменение количества товаров.
*   Просмотр содержимого корзины.
*   Автоматический расчет общей стоимости.

### 🛒 Заказы
*   Создание заказа из корзины.
*   Проверка наличия товаров на складе (включая проверки внутри транзакции).
*   Проверка и списание баланса пользователя.
*   Списание количества товаров со склада.
*   Очистка корзины после успешного заказа.
*   Атомарные транзакции для обеспечения целостности данных при создании заказа.
*   Логирование информации об успешном создании заказа.

## 🚀 Как запустить проект

Проект запускается с использованием Docker Compose. Убедитесь, что у вас установлен Docker и Docker Compose.

1.  **Клонируйте репозиторий** (в данном случае, код уже находится в рабочем каталоге).
2.  **Настройте переменные окружения**: Файл `.env` создан и содержит необходимые параметры. Используйте значения из `.env.template` как пример, если требуется изменение.
3.  **Запустите контейнеры**:
    ```bash
    docker-compose up --build -d
    ```
    Эта команда соберет Docker-образы и запустит контейнеры базы данных и веб-приложения в фоновом режиме.

4.  **Выполните миграции базы данных**:
    ```bash
    docker-compose exec web python manage.py migrate
    ```

5.  **Создайте суперпользователя (администратора)**:
    ```bash
    docker-compose exec web python manage.py shell -c "
    import os
    from django.contrib.auth import get_user_model
    from django.db import IntegrityError

    User = get_user_model()
    email = 'admin@example.com'
    password = 'adminSecurePassword123!'

    try:
        if not User.objects.filter(email=email).exists():
            User.objects.create_superuser(email=email, password=password)
            print(f'Superuser \"{email}\" created successfully.')
        else:
            print(f'Superuser \"{email}\" already exists.')
    except IntegrityError:
        print(f'IntegrityError: Superuser \"{email}\" might already exist or there was a database issue.')
    "
    ```
    *   **Email**: `admin@example.com`
    *   **Пароль**: `adminSecurePassword123!`

6.  **Используйте API**:
    *   API доступно по адресу `http://localhost:8000/api/v1/`.
    *   Аутентификация осуществляется через JWT (эндпоинты `/api/v1/users/register/`, `/api/v1/users/login/`, `/api/v1/users/token/refresh/`).

## 📜 Документация API

Автоматически сгенерированная документация API (Swagger/OpenAPI) доступна по адресу:
`http://localhost:8000/api/schema/swagger-ui/`

---
*Проект разработан в рамках тестового задания для стажировки WB tech (Wildberries&RUSS).*